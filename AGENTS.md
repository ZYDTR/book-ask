# 读书提问项目

本项目代码仓库公开，应用供个人本地使用。保留 Apple Books、原书和已有中文脚注。除用户模型 API 外软件免费。

- `src/` 放 Swift 应用；`scripts/` 放 Python 3 标准库工具与 shell 入口；`tests/` 放离线测试。
- 构建使用本机 Swift / AppKit，不依赖付费服务或额外包。
- 每个实施阶段更新 `docs/working.md`。仅在获得提交或推送授权后分阶段 commit/push；禁止自行添加其他远端或发布安装包。
- 密钥从既有私有凭据读取，配置仅保存 locator。不得输出、复制入源码或提交密钥。
- 测试通过需包含 Apple Books 内真实拖选、系统入口传递选区、Gemini Flash 实际回答和可继续追问的 UI 证据。
- 不用伪选区或固定回答冒充 E2E。API smoke test 与真正 UI 验收分别记录。
- 不改服务器、Books 数据库、输入法、豆包或其他全局快捷键。仅安装本项目用户级应用和服务入口。

## 产品与设计协作

用户在2026-09-24明确要求：Agent必须主动思考、提出判断和更好的办法，不能只顺从指令，等用户反复发现问题和纠偏。

- 讨论方案前先说清用户要解决的问题和关键假设，区分产品目标、技术上可取得的信息、推测和已验证事实。例如书/章节可用于定位正文，但不能直接代表词义相同或不同。
- 对有实质取舍的决定，主动比较少量可行方案，给出自己的推荐、理由和主要代价；不要把所有设计问题都退回给用户作选择，也不要为显得全面罗列无关选项。
- 对用户方案有疑虑时直接说明具体失败场景，并提出更简单或更可靠的替代办法。用户修正方向后，也要检查新方案的代价与矛盾，不能机械地马上赞同。
- 主动检查用户是否能理解界面正在做什么：数据来源、语境、等待、失败、保留/覆盖和开关之间的关系。不能把会改变答案含义的重要信息只藏在悬停提示里。
- 技术可行性用实际输入和目标场景验证；不以单本书、解析脚本、组件测试代替安装包的自动能力，不把实验成功写成产品已实现。
- 区分“已确认要求”“Agent建议”“待验证假设”。主动提出建议不等于擅自实施；用户要求先讨论或只冷测时，保留正式软件和数据，先把决定做成可审阅结果。
- 后续汇报应体现新增判断、发现和证据，而不是只重复用户的话或罗列执行动作；尽量减少用户纠偏和重复说明的负担。
