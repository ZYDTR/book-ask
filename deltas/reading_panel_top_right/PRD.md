# 划词解释窗口固定在屏幕右上角

2026-09-20 用户表示应用已很好，要求窗口固定在刚拖到的屏幕右上角；同时询问现有网关还有哪些 Gemini Flash。通过当前应用 UI 确认所指是标题为「读书提问」的划词解释主窗口（500×539），不是独立的历史词本列表。原偏好读到 frame `1228 545 500 539 0 0 1728 1084`，恰好贴齐当前可用区域的右边和顶边。

## 合同与实现边界

每次启动、重新打开或新选区前置时，把主窗口放在所在屏幕可用区域右上角，避开菜单栏。保留 500×539 默认尺寸、临时缩放、普通层级、点击外部让出、现有取词与自动解释、用户提示词及历史。临时拖动后，下次打开重新回到右上角；不持续用定时器拉回。屏幕变化使用窗口所在显示器，缺失时用当前主屏；不把 1728 等机器尺寸写死。

改动仅 main.swift 的窗口初始化/呈现及位置日志。无需修改历史词本窗口、模型配置、网关、原书或取词链路。此小改动用本文件承载需求、实施与验证，不新增其他文档。

## 验证

构建通过；最终安装版通过 UI 启动/显式重新显示后，日志 panelFrame.maxX/maxY 等于 visibleFrame.maxX/maxY；临时移动后再显示能恢复。核对授权、自动解释与用户模板仍与变更前一致。此窗口修改的验证不能冒充上一 Delta 所有全屏/取词测试通过。

## 网关只读核对

2026-09-20 09:52 +08:00，当前配置为 `http://10.0.0.217:4000/v1`，model `gemini-3.7-flash`。GET /models 列出 `gemini-3.5-flash`、`gemini-3.7-flash`、`gemini-3.1-pro-preview`、`gemini-3-pro-image`。这是网关别名列表，不是官方命名核验或成功推理测试；本轮不切换模型、不发付费推理请求。去密证据在 evidence/reading_panel_top_right_20260920/baseline_and_models.json。

实施与 UI 结果：**完成并安装**。候选 SHA-256 `2097b5a5a0405af78bdeb610661f348b180d8a9ababbbc340f03e560a4243f31` 与安装一致。构建通过；真实 UI 启动与菜单「显示读书提问」均记录 frame `{{1228, 545}, {500, 539}}`，screenVisibleFrame `{{0, 81}, {1728, 1003}}`，两个顶边与右边逐项核对相等。临时移动的 CUA drag 没改变实际保存的 frame，未把此动作计作移开后回归测试。呈现函数每次计算当前 visibleFrame，代码行为不依赖已保存绝对坐标。

同一应用的辅助功能授权已刷新且日志 true；autoExplainEnabled、copyCaptureEnabled、explanationPrompt 与变更前逐项一致。当前模型仍 gemini-3.7-flash，未进行付费模型调用。证据：evidence/reading_panel_top_right_20260920/verification.json。未改取词、词本历史或原书，无提交/发布。
